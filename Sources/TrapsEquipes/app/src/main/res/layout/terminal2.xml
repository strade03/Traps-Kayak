<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#000000"
    android:orientation="vertical">

    <Spinner
        android:id="@+id/spinner1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="#000000"
        android:textSize="40sp"
        android:textStyle="bold" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:id="@+id/penalty_groups_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >
            <!-- Les en-têtes de porte et les lignes de jugement seront ajoutés ici -->
        </LinearLayout>
    </ScrollView>

    <!-- Les boutons Previous/Next restent en bas -->
    <LinearLayout
        android:id="@+id/rowButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <!-- android:layout_weight="1" sur ce LinearLayout n'est pas utile
             s'il n'est pas dans un parent avec une dimension définie (ex: 0dp)
             et d'autres éléments partageant le poids.
             Pour une hauteur fixe, utilisez layout_height="wrap_content" ou une valeur dp.
        -->

        <ImageButton
            android:id="@+id/previous_button"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:adjustViewBounds="true"
            android:background="#000000"
            android:scaleType="centerInside"
            android:src="@drawable/button_prevbib" />

        <TextView
            android:id="@+id/textView1"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:textSize="25sp" /> <!-- Ce TextView semble être un espaceur ? -->

        <ImageButton
            android:id="@+id/next_button"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:adjustViewBounds="true"
            android:background="#000000"
            android:scaleType="centerInside"
            android:src="@drawable/button_nextbib" />
    </LinearLayout>

</LinearLayout>